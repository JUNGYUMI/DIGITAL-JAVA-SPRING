<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.springpro.dao.BoardDao">
	<select id="getBoardList" resultType="kr.green.springpro.vo.BoardVo">
		select * from board where isDel = 'N'
		order by num desc
	</select>
	<select id="getBoard" resultType="kr.green.springpro.vo.BoardVo">
		select * from board where num = #{num}
	</select>
	
	<insert id="registerBoard">
		insert into 
			board(title, writer, content)
			values(#{board.title}, #{board.writer}, #{board.content})
	</insert>
	<update id="updateBoard">
		update board
			set
				title = #{board.title},
				content = #{board.content}
			where
				num = #{board.num}
				
	</update>
</mapper>